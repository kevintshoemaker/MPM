% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_scen.R
\name{gen_scen}
\alias{gen_scen}
\title{Build a scenario list for MPM functions}
\usage{
gen_scen(fysurv, jsurv, asurv, fec, dur, ramp)
}
\arguments{
\item{fysurv}{Numeric scalar. First-year survival probability.}

\item{jsurv}{Either a numeric vector of mean juvenile survival rates (one
per stage) or a data frame with columns \code{mean} and optionally \code{min} when
a survival ramp is desired.}

\item{asurv}{Numeric scalar. Adult survival probability.}

\item{fec}{Numeric scalar. Annual fecundity (female offspring per adult
female per year, excluding first-year survival).}

\item{dur}{Either a numeric vector of stage durations (one per juvenile
stage) or a data frame with columns \code{dur}, \code{min}, and \code{max} for variable
stage durations and number of rows equal to the number of stages.}

\item{ramp}{Logical. If \code{TRUE}, juvenile survival increases monotonically
within each stage (a "ramp"), constrained to match the specified mean.
Only used when \code{jsurv} is supplied as a vector; if \code{jsurv} is already a
data frame this argument is ignored.}
}
\value{
A named list with elements \code{fysurv}, \code{jsurv}, \code{asurv}, \code{fec},
and \code{dur}.
}
\description{
A convenience wrapper that packages life-history parameters into a named
list accepted by \code{\link[=do_aas]{do_aas()}}, \code{\link[=do_fas]{do_fas()}}, and \code{\link[=do_unroll]{do_unroll()}}.
}
\examples{
# Single juvenile stage, fixed duration, no ramp
scen <- gen_scen(
  fysurv = 0.45,
  jsurv  = 0.75,
  asurv  = 0.96,
  fec    = 1.5,
  dur    = 9,
  ramp   = FALSE
)

# Two juvenile stages with survival ramp
scen2 <- gen_scen(
  fysurv = 0.45,
  jsurv  = data.frame(mean = c(0.5, 0.8), min = c(0.25, 0.65)),
  asurv  = 0.95,
  fec    = 2.29,
  dur    = c(3, 4),
  ramp   = TRUE
)

}
