% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_unroll.R
\name{do_unroll}
\alias{do_unroll}
\title{Construct an age-based MPM by unrolling stage durations}
\usage{
do_unroll(fys, js, as, f, t)
}
\arguments{
\item{fys}{Numeric scalar. First-year survival (age 0 to age 1).
In a pre-breeding census model this is folded into the fertility term.}

\item{js}{Data frame of juvenile survival rates, one row per juvenile
stage. Either one column (\code{mean}) for constant survival, or two columns
(\code{mean}, \code{min}) when a survival ramp is requested.}

\item{as}{Numeric scalar. Adult (final-stage) survival rate.}

\item{f}{Numeric scalar. Annual fecundity — female offspring produced per
adult female per year. Does \strong{not} include first-year survival adjustment
(that is handled via \code{fys}).}

\item{t}{Data frame of stage durations, one row per juvenile stage. Either
one column (\code{dur}) for fixed duration, or three columns (\code{dur}, \code{min},
\code{max}) for variable durations.}
}
\value{
A square numeric age-based matrix of dimension
\verb{(total ages) x (total ages)}. The dominant eigenvalue equals lambda.
}
\description{
Converts life-history parameters into a full age-based (Leslie-type)
matrix by "unrolling" each juvenile stage into its constituent annual age
classes. Supports fixed or variable stage durations and optional
within-stage survival ramps. This is generally the recommended method for
modeling any age-structured population.
}
\details{
\strong{Pre-breeding census model.} All fertility terms implicitly assume that
the census occurs just before the breeding event, so the fertility
row of the matrix is \code{f * fys} (fecundity × first-year survival). The
matrix dimension equals \verb{sum(stage durations) + 1} (the \code{+1} is for the
adult stage).

\strong{Variable stage durations.} When \code{t} has three columns (\code{dur}, \code{min},
\code{max}), the probability distribution of ages at stage transition is
computed via maximum-entropy optimization (\code{\link[=vari_dur]{vari_dur()}}) and convolved
across stages.

\strong{Survival ramp.} When \code{js} has a \code{min} column, annual survival within
each stage follows a monotonically increasing or decreasing curve
(computed by \code{\link[=ramp_fun]{ramp_fun()}}) that starts at the specified minimum (or the
previous stage's maximum if \code{min = NA}) and ends at the minimum survival
of the subsequent stage, while matching the specified mean.
}
\examples{
# Fixed duration, no ramp
scen <- gen_scen(fysurv=0.45, jsurv=0.75, asurv=0.96,
                 fec=1.5, dur=9, ramp=FALSE)
mat <- do_unroll(scen$fysurv, scen$jsurv, scen$asurv, scen$fec, scen$dur)
popbio::lambda(mat)

# Variable stage duration with survival ramp
scen7 <- gen_scen(
  fysurv = 0.45,
  jsurv  = data.frame(mean = 0.75, min = 0.5),
  asurv  = 0.95,
  fec    = 2.29,
  dur    = data.frame(dur=9, min=6, max=13),
  ramp   = TRUE
)
mat7 <- do_unroll(scen7$fysurv, scen7$jsurv, scen7$asurv,
                  scen7$fec, scen7$dur)
popbio::lambda(mat7)

}
\references{
Kendall, B.E., Fujiwara, M., Diaz-Lopez, J., Schneider, S., Voigt, J. and Wiesner, S., 2019.
Persistent problems in the construction of matrix population models.
\emph{Ecological modelling}, 406, pp.33-43.
}
\seealso{
\code{\link[=do_aas]{do_aas()}}, \code{\link[=do_fas]{do_fas()}}, \code{\link[=ramp_fun]{ramp_fun()}}, \code{\link[=vari_dur]{vari_dur()}}
}
