% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_fas.R
\name{do_fas}
\alias{do_fas}
\title{Construct a stage-based MPM using the "Flat Age-Within-Stage" (FAS) method (not recommended)}
\usage{
do_fas(fys, js, as, f, t)
}
\arguments{
\item{fys}{Numeric scalar. First-year survival (age 0 to age 1).
In a pre-breeding census model this is folded into the fertility term.}

\item{js}{Data frame of juvenile survival rates, one row per juvenile
stage. Either one column (\code{mean}) for constant survival, or two columns
(\code{mean}, \code{min}) when a survival ramp is requested.}

\item{as}{Numeric scalar. Adult (final-stage) survival rate.}

\item{f}{Numeric scalar. Annual fecundity — female offspring produced per
adult female per year. Does \strong{not} include first-year survival adjustment
(that is handled via \code{fys}).}

\item{t}{Data frame of stage durations, one row per juvenile stage. Either
one column (\code{dur}) for fixed duration, or three columns (\code{dur}, \code{min},
\code{max}) for variable durations.}
}
\value{
A square numeric stage-based matrix.
}
\description{
Implements the "Fixed Age at Stage" (FAS) approximation, in which exactly
\code{1/t} of each stage is assumed to transition each year regardless of
lambda. This method is known to be incorrect in most cases and is included
for comparison purposes only.
}
\section{Warning}{

This method systematically overestimates population growth rates and
\strong{should not be used} for management or conservation applications. It is
retained here for benchmarking against legacy analyses (e.g., the Gopher
Tortoise SSA model).
}

\examples{
scen <- gen_scen(fysurv=0.45, jsurv=0.75, asurv=0.96, fec=1.5,
                 dur=9, ramp=FALSE)
mat <- do_fas(scen$fysurv, scen$jsurv, scen$asurv, scen$fec, scen$dur)
popbio::lambda(mat)   # compare with do_aas()

}
\references{
Kendall, B.E., Fujiwara, M., Diaz-Lopez, J., Schneider, S., Voigt, J. and Wiesner, S., 2019.
Persistent problems in the construction of matrix population models.
\emph{Ecological modelling}, 406, pp.33-43.
}
\seealso{
\code{\link[=do_aas]{do_aas()}}, \code{\link[=do_unroll]{do_unroll()}}
}
